# Figma API Access Token
# Figma 설정 → Account → Personal access tokens에서 생성
# https://www.figma.com/settings 에서 "Generate new token" 클릭
FIGMA_ACCESS_TOKEN=figd_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google OAuth (서버용)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth (클라이언트용 - 프론트엔드에서 사용)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# JWT 설정 (최소 32자 이상)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long

# 도메인 제한 (선택) - 특정 회사 도메인만 허용
# 설정하지 않으면 모든 Google 계정 허용
ALLOWED_EMAIL_DOMAIN=barun.com

# S3 설정 (이미지 업로드용)
USE_S3=false
AWS_REGION=ap-northeast-2
AWS_S3_BUCKET=your-bucket-name
AWS_S3_PREFIX=card-templates
AWS_CLOUDFRONT_DOMAIN=your-cloudfront-domain.cloudfront.net

# bdc-web 백오피스 API 연동
# 템플릿을 bdc-web에 자동 등록하기 위한 설정
BDC_WEB_API_URL=https://api.barunsoncard.com
BDC_WEB_API_KEY=your-api-key
